@{
    ViewBag.Title = Html.LanguageString("Leaderboard.LeaderboardTitle");
}


@section AdminHeader {
    @(
 Bundle.JavaScript()
            .Add("~/Scripts/leaderboard.js")
            .MvcRender("~/scripts/cache/leaderboard-combined_#.js")
    )
}

<div class="tabbable">
    <ul class="nav nav-tabs" id="myTab">
        <li class="active"><a data-toggle="tab" href="#leaderboard">Leaderboard</a></li>
    </ul>
    <div class="tab-content">
        <div class="tab-pane active" id="leaderboard">
            <div class="row-fluid">
                <div class="span12">
                    <div class="row-fluid">
                        <div class="span6 thisweeksstars">
                            <h2>@Html.LanguageString("Leaderboard.ThisWeeksStars")</h2>

                            <div class="leaderboardholders thisweekleaderboard">
                                <span class="ajaxspinner" title="@Html.LanguageString("Ajax.PleaseWait")">@Html.LanguageString("Ajax.PleaseWait")</span>
                            </div>

                        </div>
                        <div class="span6 thisyearsstars">
                            <h2>@Html.LanguageString("Leaderboard.ThisYearsStars")</h2>
                            <div class="leaderboardholders alltimeleaderboard">
                                <span class="ajaxspinner" title="@Html.LanguageString("Ajax.PleaseWait")">@Html.LanguageString("Ajax.PleaseWait")</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
